#include <bits/stdc++.h>
using namespace std;
#define int long long

void solve(){
    int a = 0;
    cout<< (a++) + a << '\n';
}

signed main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t;
    t = 1;
    for(int i = 1; i <= t; i++){
        solve();
    }
}